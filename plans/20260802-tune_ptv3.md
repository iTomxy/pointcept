# Goal

On 2 August 2026,
[configs/ribsegv2/semseg-pt_v3m1_0_base.py](../configs/ribsegv2/semseg-pt_v3m1_0_base.py)
yields a reasonable baseline on the Ribsegv2 CT rib segmentation task:
- log path: `exp/ribsegv2/semseg-pt_v3m1_0_base/`

| | all | FG classes |
| - | - | - |
| iou | 83.45 | 82.89 |
| dice | 88.6 | 88.19 |
| precision | 90.41 | 90.1 |
| recall | 88.14 | 87.7 |
| specificity | 99.62 | 99.93 |
| accuracy | 99.74 | 99.83 |

Notes that the FG metrics (i.e. ignoring background) is what we mainly care about as a final metrics,
while including the background can still be useful for analysing.
Next, we shall tune the model with common tricks to see the limit of PTv3 on this task.

Goal 2: run 3 repeated experiments with the tuned optimal recipe.

## Performance Table

- All values metrics except accuracy are foreground averages (ignore background class 0).
- Cell format: `<val-set-value> / <test-set-value>`, in percent.
- Grouped comparisons list validation results in log-path order; step 1b gives ranges across all four seeds (including `in_feat-whu1`), and step 2 reports `scale`.
- Goal 2 stderr is the sample standard deviation across the three seeds divided by √3, calculated before rounding.

| | log path | iou | dice | recall | precision | specificity | acc | label acc | label acc 1st | label acc inter | label acc last |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| [baseline](../exp/ribsegv2/semseg-pt_v3m1_0_base/) | [exp/ribsegv2/semseg-pt_v3m1_0_base/](../exp/ribsegv2/semseg-pt_v3m1_0_base/) | - / 82.89 | - / 88.19 | - / 90.1 | - / 87.7 | - / 99.93 | - / 99.83 | - / 90.97 | - / 93.31 | - / 92.06 | - / 78.89 |
| [step 1](../exp/ribsegv2/in_feat-whu1/) — input features (whu1) | [exp/ribsegv2/in_feat-whu1/](../exp/ribsegv2/in_feat-whu1/) | 84.92 / - | 90.41 / - | 90.16 / - | 93.80 / - | - / - | - / - | 93.80 / - | 97.95 / - | 95.34 / - | 73.44 / - |
| step 1b — noise floor (4 seeds, [s1](../exp/ribsegv2/noise-whu1-s1/), [s2](../exp/ribsegv2/noise-whu1-s2/), [s3](../exp/ribsegv2/noise-whu1-s3/)) | [s0 (whu1)](../exp/ribsegv2/in_feat-whu1/) / [s1](../exp/ribsegv2/noise-whu1-s1/) / [s2](../exp/ribsegv2/noise-whu1-s2/) / [s3](../exp/ribsegv2/noise-whu1-s3/) | 85.02–85.42 | 90.21–90.61 | 89.45–90.21 | 93.29–94.15 | - | - | 93.29–94.15 | 96.58–98.63 | 94.44–95.34 | 73.44–81.25 |
| step 2 — aug singles ([droprib](../exp/ribsegv2/aug-droprib/), [hujitter](../exp/ribsegv2/aug-hujitter/), [hushift](../exp/ribsegv2/aug-hushift/), [roty](../exp/ribsegv2/aug-roty/), [rotz](../exp/ribsegv2/aug-rotz/), [scale](../exp/ribsegv2/aug-scale/), [truncrib](../exp/ribsegv2/aug-truncrib/)) | best: [scale](../exp/ribsegv2/aug-scale/) / [truncrib](../exp/ribsegv2/aug-truncrib/) / [roty](../exp/ribsegv2/aug-roty/) | 87.36 / - | 92.32 / - | 92.27 / - | 96.53 / - | - / - | - / - | 96.53 / - | 99.32 / - | 97.95 / - | 78.12 / - |
| step 2b — aug combos (lr-pinned) | [all5](../exp/ribsegv2/augc-all5/) / [scale](../exp/ribsegv2/augc-scale/) / [all5alt](../exp/ribsegv2/augc-all5alt-lrpin/) / [top3](../exp/ribsegv2/augc-top3-lrpin/) | 88.64 / 87.98 / 87.92 / 87.61 | 93.36 / 92.69 / 92.58 / 92.30 | 93.09 / 92.44 / 92.15 / 91.83 | 97.18 / 96.20 / 96.45 / 95.95 | - / - / - / - | - / - / - / - | 97.18 / 96.20 / 96.45 / 95.95 | 98.63 / 97.95 / 99.32 / 97.95 | 98.46 / 97.81 / 97.70 / 97.40 | 82.03 / 77.34 / 79.69 / 78.91 |
| step 3 — grid x windows x prec (lr-pinned) | [3mm red fp32](../exp/ribsegv2/augc-all5/) / [4mm full bf16](../exp/ribsegv2/grid-g4p1024-lrpin/) / [3mm full bf16](../exp/ribsegv2/grid-g3p1024-lrpin/) / [4mm red fp32](../exp/ribsegv2/grid-g4p128nf-lrpin/) | 88.64 / 88.36 / 88.18 / 87.99 | 93.36 / 93.14 / 92.90 / 92.80 | 93.09 / 92.73 / 92.46 / 92.24 | 97.18 / 96.80 / 97.14 / 96.61 | - / - / - / - | - / - / - / - | 97.18 / 96.80 / 97.14 / 96.61 | 98.63 / 97.95 / 99.32 / 99.32 | 98.46 / 98.22 / 98.53 / 97.80 | 82.03 / 80.47 / 78.91 / 80.47 |
| step 4 — opt knobs (lr-pinned vs ref) | [ref](../exp/ribsegv2/opt-ref/) / [dp0.5](../exp/ribsegv2/opt-dp0.5/) / [ls0.1](../exp/ribsegv2/opt-ls0.1/) / [wd5e-2](../exp/ribsegv2/opt-wd5e-2/) | 88.23 / 88.68 / 88.13 / 88.26 | 93.00 / 93.36 / 92.91 / 93.00 | 92.50 / 92.92 / 92.29 / 92.52 | 96.65 / 97.22 / 96.73 / 96.79 | - / - / - / - | - / - / - / - | 96.65 / 97.22 / 96.73 / 96.79 | 99.32 / 99.32 / 99.32 / 98.63 | 97.91 / 98.56 / 98.18 / 98.02 | 79.69 / 79.69 / 77.34 / 81.25 |
| [FINAL](../exp/ribsegv2/final/) (100 epochs) | [exp/ribsegv2/final/](../exp/ribsegv2/final/) | 87.96 / 87.96 | 92.64 / 92.64 | 92.14 / 92.14 | 94.14 / 94.14 | 99.96 / 99.96 | 99.90 / 99.90 | 97.02 / 96.12 | 99.32 / 96.50 | 98.00 / 97.32 | 83.59 / 82.59 |
| Goal 2 — [s0](../exp/ribsegv2/goal2-s0/) (seed 1) | [exp/ribsegv2/goal2-s0/](../exp/ribsegv2/goal2-s0/) | - / 87.62 | - / 92.28 | - / 92.18 | - / 94.40 | - / 99.96 | - / 99.90 | - / 95.91 | - / 95.22 | - / 97.01 | - / 84.81 |
| Goal 2 — [s1](../exp/ribsegv2/goal2-s1/) (seed 2) | [exp/ribsegv2/goal2-s1/](../exp/ribsegv2/goal2-s1/) | - / 87.42 | - / 92.22 | - / 92.78 | - / 93.46 | - / 99.95 | - / 99.89 | - / 96.20 | - / 97.45 | - / 97.10 | - / 84.81 |
| Goal 2 — [s2](../exp/ribsegv2/goal2-s2/) (seed 3) | [exp/ribsegv2/goal2-s2/](../exp/ribsegv2/goal2-s2/) | - / 87.62 | - / 92.25 | - / 92.07 | - / 94.54 | - / 99.96 | - / 99.90 | - / 95.99 | - / 96.18 | - / 97.07 | - / 84.07 |
| Goal 2 — mean ± stderr (n=3) | [s0](../exp/ribsegv2/goal2-s0/) / [s1](../exp/ribsegv2/goal2-s1/) / [s2](../exp/ribsegv2/goal2-s2/) | - / 87.55±0.07 | - / 92.25±0.02 | - / 92.34±0.22 | - / 94.13±0.34 | - / 99.96±0.00 | - / 99.90±0.00 | - / 96.03±0.09 | - / 96.28±0.65 | - / 97.06±0.03 | - / 84.57±0.25 |

Note: Goal 2 re-runs the tuned recipe (configs/ribsegv2/semseg-pt_v3m1_0_base.py)
at three fixed seeds, scored on the **test** split (157 volumes). All three seeds
land within 0.07 pt of each other on dice_fg (mean 0.9225, stderr 0.02 pt) — an
order of magnitude inside the 0.40 pt noise floor — so the +4.44 pt tuning gain is
not a seed artefact. The 3-seed test mean (dice 92.25) sits 0.39 pt below the
single FINAL read (92.64), i.e. essentially at the noise floor; precision_fg is the
only metric with visible seed spread (93.46–94.54, stderr 0.34). Helper scripts:
tmp-ai-goal2.sh (train+test one seed) and tmp-ai-goal2_results.py (this table).

- (14 Sept 2026) All log path are in ihpc tliang.

# Tuning

Refer to `~/codes/tmp.ptcloud/configs/ribsegv2/seq-tune/` for my previous tuning for DGCNN on the same task.

The full tuning order (configuration files under the above path) is:
1. baseline.py
2. physics.py
3. global_r.py
4. in_feat.py
5. aug.py
6. aug-comb.py
7. scale.py
8. arch.py
9. opt.py
10. final.py: the summary of all tuned practices, NOT a tuning step.

The PTv3 baseline already included step 1 to 3.
In this tuning plan, we do not go through the full tuning progress listed above.
Instead,
the candidate tuning space includes, in order:
1. input features/channels: other/more than the current single windowed HU intensity?
2. data augmentations & their combinations.
3. optimisation:
    a) use label smoothing in CrossEntropyLoss or not.
    b) regularisation? weight decay?

# Notes

- Compare and select models by the performance on the validation set.
    - Also test with the same tester `Ribsegv2VolumeTester` after training,
    but with split set to `val` instead of `test`,
    and compare the metrics of the output of it.

- Create any `tmp-ai*.sh` or `tmp-ai*.py` helper script as needed.
They are ignored by git and thus safe to create.

- Read [env.md](env.md) for environment statements.

# Steps

1. [x] read the sequential tuning in `~/codes/tmp.ptcloud/configs/ribsegv2/seq-tune/` for reference.

2. [x] determine: whether the tuning plan is reasonable, e.g.
do we need to add/drop some entries or adjust the tuning order,
and explain why.

3. [x] after confirming the tunning sequence,
generate one configuration file for each tunning step under
[configs/ribsegv2/seq-tune/](../configs/ribsegv2/seq-tune/),
and create as many tmp-ai*.sh
(e.g. tmp-ai-1.sh, tmp-ai-tune_label_smoothing)
to run them as you need.
    - Note: create the config for next tuning experiment after its preceding tuning is finished,
    cuz it should inherit the new concluded practice from that precedent.

4. [x] Step 1 — input features: completed. All four HU representations within 0.33 pt (inside noise floor). Keep `whu1`. Noise calibration (4 seeds): noise floor = 0.40 pt.

5. [x] Step 2 — augmentation singles & combinations: completed. **Augmentation is the only lever that moved the number: +2.96 pt.** `droprib` hurts (-0.58). `all5` (all five positives in sequence) beats `scale` alone by 0.67 pt. Sequential rotations beat `RandomApply` choose-one by 0.79 pt (was originally claimed 2.7 pt, but 70% was lr artefact).

6. [x] Step 3 — grid size x attention window x precision: completed. **Keep 3mm, keep fp32.** `bf16` costs 0.98 pt, full windows gain 0.52 pt, 4mm costs 0.56 pt. `enc_patch_size` is NOT irrelevant — larger windows worth +0.52 pt at matched precision; the original -0.02 tie was two opposing errors cancelling. In practice fp32 forces reduced windows (no flash = materialised attention = OOM at 8x), so +0.52 never covers -0.98.

7. [x] Step 4 — optimisation knobs: completed. **Every knob is a tie: label smoothing, weight decay (100x sweep), drop_path all inside noise floor.** `dp0.5` looked like +0.36 winner but second seed reproduced noise floor (0.41 pt spread, mean +0.16). Training hook mIoU is misleading — it rewards dense interior of large ribs; tester weights all volumes equally at full resolution.

8. [x] FINAL — tuned recipe at 100 epochs on TEST split: completed. `dice_fg 0.8819 -> 0.9264 (+4.44 pt)`, `iou_fg +5.07`, `label_acc +5.15`. Mislabelled rib points 1,895,622 -> 206,294 (89% reduction). Val 0.9306 vs test 0.9264 (0.42 pt gap ≈ noise floor): ~35 runs of selection on val did not overfit. 100 epochs bought only +0.05 pt over 50 epochs.

Goal 2:

1. [x] Run 3 repeated experiments with the tuned optimal recipe,
which is recorded in the canonical [configs/ribsegv2/semseg-pt_v3m1_0_base.py](../configs/ribsegv2/semseg-pt_v3m1_0_base.py).
     - [x] Record results of each run in [Performance Table](#performance-table).
     - [x] After 3 runs finish, calculate average & stderr of metrics, and record in [Performance Table](#performance-table) as a new line.

<!-- results:begin -->

# Results

Generated by `tmp-ai-results_table.py` -- rerun it after every step.
All rows below the baseline are **50 epochs**, scored by `Ribsegv2VolumeTester` over the **val** split (73 volumes, full resolution, averaged per volume, foreground-only) from `<log path>/val-Ribsegv2VolumeTester.json`, with per-volume detail in the sibling `-per_volume.jsonl`. The **noise floor is 0.40 pt**; treat anything smaller as a tie.

## Caveat: the tuned lr is not stable

Steps 1 and 1b pin lr at 0.005719 for every run, so those rows are clean. From step 2 onward each run re-tunes its own lr with `tools/lr_range_test.py`, and the `lr` column shows the result spans **0.0045 to 0.0183, a 4x range**, across runs that were supposed to differ only in the factor under test.

That spread is mostly not a real per-arm optimum. Comparing the range-test curves of `augc-all5` (lr 0.0051) and `opt-ref` (lr 0.0115) -- configs that resolve byte-identical -- the two loss curves agree to 3 decimals up to lr 0.012 and then diverge: one hit a transient loss spike just past 0.012 and never recovered its minimum, the other did not. Both curves agree on where training actually blows up (~0.08), but the `lr_min_loss`/`lr_knee` heuristic latches onto the local blip instead, so a single noisy pass moves the recommendation by 2.25x.

Practical effect: comparisons between rows with similar lr are sound, and rows whose lr differs by >2x carry an unquantified confound -- flagged in the notes where it matters. `opt-ref` measures the size of that confound directly, since it is the `all5` recipe at 2.25x the lr and nothing else changed.

## Baseline (100 epochs, before tuning)

Config: [configs/ribsegv2/semseg-pt_v3m1_0_base.py](../configs/ribsegv2/semseg-pt_v3m1_0_base.py)

| mode | log path | batch | lr | dice_fg | iou_fg | recall_fg | label_acc | Δ dice_fg |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `-` | `exp/ribsegv2/semseg-pt_v3m1_0_base/` | 4 | 0.005719 | **0.8819** | 0.8289 | 0.8770 | 0.9097 |  |

Not comparable to the rows below: 100 epochs, and scored on the **test** split. Everything after it is 50 epochs on **val**.

## Step 1 -- input features

Config: [configs/ribsegv2/seq-tune/in_feat.py](../configs/ribsegv2/seq-tune/in_feat.py) -- the `mode` column is its `TUNE_MODE`

| mode | log path | batch | lr | dice_fg | iou_fg | recall_fg | label_acc | Δ dice_fg |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `best+xyz` | `exp/ribsegv2/in_feat-best+xyz/` | 4 | 0.01024 | **0.8996** | 0.8500 | 0.8970 | 0.9303 | -0.45 |
| `best+z` | `exp/ribsegv2/in_feat-best+z/` | 4 | 0.0183 | **0.8656** | 0.8053 | 0.8564 | 0.8790 | -3.85 |
| `nhu` | `exp/ribsegv2/in_feat-nhu/` | 4 | 0.005719 | **0.9054** | 0.8532 | 0.8940 | 0.9216 | +0.13 |
| `nhu+whu3` | `exp/ribsegv2/in_feat-nhu+whu3/` | 4 | 0.005719 | **0.9072** | 0.8561 | 0.9007 | 0.9366 | +0.31 |
| `whu1` | `exp/ribsegv2/in_feat-whu1/` | 4 | 0.005719 | **0.9041** | 0.8492 | 0.9016 | 0.9380 | ref |
| `whu3` | `exp/ribsegv2/in_feat-whu3/` | 4 | 0.005719 | **0.9039** | 0.8529 | 0.8982 | 0.9291 | -0.02 |

All four HU representations land within 0.33 pt, i.e. inside the noise floor. Adding position as a feature does not help either. **Conclusion: keep `whu1`.** `best+z`/`best+xyz` were run before the lr rule was fixed (see the lr caveat below) and their lr is 2-3.6x the rest, so their deficit is not interpretable.

## Step 1b -- noise calibration

Config: [configs/ribsegv2/seq-tune/in_feat.py](../configs/ribsegv2/seq-tune/in_feat.py) -- the `mode` column is its `TUNE_MODE`

| mode | log path | batch | lr | dice_fg | iou_fg | recall_fg | label_acc | Δ dice_fg |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `whu1-s1` | `exp/ribsegv2/noise-whu1-s1/` | 4 | 0.005719 | **0.9040** | 0.8522 | 0.9021 | 0.9415 | -0.01 |
| `whu1-s2` | `exp/ribsegv2/noise-whu1-s2/` | 4 | 0.005719 | **0.9021** | 0.8502 | 0.8945 | 0.9351 | -0.20 |
| `whu1-s3` | `exp/ribsegv2/noise-whu1-s3/` | 4 | 0.005719 | **0.9061** | 0.8542 | 0.8999 | 0.9329 | +0.20 |

Four seeds of one identical config (`in_feat-whu1` is seed 0). The range over these four runs is the **noise floor: 0.40 pt**. Any delta below it is reported as a tie.

## Step 2 -- augmentation, one at a time

Config: [configs/ribsegv2/seq-tune/aug.py](../configs/ribsegv2/seq-tune/aug.py) -- the `mode` column is its `TUNE_MODE`

| mode | log path | batch | lr | dice_fg | iou_fg | recall_fg | label_acc | Δ dice_fg |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `droprib` | `exp/ribsegv2/aug-droprib/` | 4 | 0.005102 | **0.8983** | 0.8450 | 0.8927 | 0.9247 | -0.58 |
| `hujitter` | `exp/ribsegv2/aug-hujitter/` | 4 | 0.005102 | **0.9038** | 0.8520 | 0.8930 | 0.9308 | -0.04 |
| `hushift` | `exp/ribsegv2/aug-hushift/` | 4 | 0.005102 | **0.9100** | 0.8603 | 0.9005 | 0.9350 | +0.59 |
| `roty` | `exp/ribsegv2/aug-roty/` | 4 | 0.006067 | **0.9190** | 0.8707 | 0.9092 | 0.9570 | +1.49 |
| `rotz` | `exp/ribsegv2/aug-rotz/` | 4 | 0.01368 | **0.9101** | 0.8600 | 0.9061 | 0.9437 | +0.60 |
| `scale` | `exp/ribsegv2/aug-scale/` | 4 | 0.004543 | **0.9232** | 0.8736 | 0.9227 | 0.9653 | +1.91 |
| `truncrib` | `exp/ribsegv2/aug-truncrib/` | 4 | 0.008118 | **0.9166** | 0.8676 | 0.9131 | 0.9527 | +1.25 |

`droprib` is the only one that hurts: it deletes whole rib pairs mid-cage, an anatomy absent from the data, while `truncrib` cuts along z like a real partial field of view. `jitter` was excluded by design -- PTv3 keys off `grid_coord`, so sub-cell displacement is invisible to attention and only inflates the point count.

## Step 2b -- augmentation combinations

Config: [configs/ribsegv2/seq-tune/aug-comb.py](../configs/ribsegv2/seq-tune/aug-comb.py) -- the `mode` column is its `TUNE_MODE`

| mode | log path | batch | lr | dice_fg | iou_fg | recall_fg | label_acc | Δ dice_fg |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `all5` | `exp/ribsegv2/augc-all5/` | 4 | 0.005102 | **0.9336** | 0.8864 | 0.9309 | 0.9718 | +2.95 |
| `all5alt` | `exp/ribsegv2/augc-all5alt/` | 4 | 0.01368 | **0.9070** | 0.8559 | 0.9021 | 0.9393 | +0.29 |
| `all5alt-lrpin` | `exp/ribsegv2/augc-all5alt-lrpin/` | 4 | 0.005102 | **0.9258** | 0.8792 | 0.9215 | 0.9645 | +2.16 |
| `scale` | `exp/ribsegv2/augc-scale/` | 4 | 0.005102 | **0.9269** | 0.8798 | 0.9244 | 0.9620 | +2.28 |
| `top3` | `exp/ribsegv2/augc-top3/` | 4 | 0.009653 | **0.9238** | 0.8763 | 0.9183 | 0.9618 | +1.97 |
| `top3-lrpin` | `exp/ribsegv2/augc-top3-lrpin/` | 4 | 0.005102 | **0.9230** | 0.8761 | 0.9183 | 0.9595 | +1.88 |

Both arms have been re-run at `all5`'s lr. `top3` moved only -0.09 pt, so **"stacking the top-3 singles bought nothing over `scale` alone" stands**. `all5alt` moved **+1.88 pt** (0.9070 -> 0.9258): its 0.01368 lr was most of the gap, so the original "sequential rotations beat `RandomApply` choose-one by 2.7 pt" is wrong -- **the real margin is 0.79 pt**, about 2x the noise floor. At matched lr the ranking is `all5` 0.9336 > `scale` 0.9269 > `all5alt` 0.9258 > `top3` 0.9230, so **`all5` is still the winner**, by 0.67 pt over the runner-up. Read the `-lrpin` rows, not the originals, for every arm that has one.

## Step 3 -- grid size x attention window x precision

Config: [configs/ribsegv2/seq-tune/grid.py](../configs/ribsegv2/seq-tune/grid.py) -- the `mode` column is its `TUNE_MODE`

| mode | log path | batch | lr | dice_fg | iou_fg | recall_fg | label_acc | Δ dice_fg |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `g3p1024` | `exp/ribsegv2/grid-g3p1024/` | 4 | 0.01084 | **0.9237** | 0.8764 | 0.9184 | 0.9633 | -1.00 |
| `g3p1024-lrpin` | `exp/ribsegv2/grid-g3p1024-lrpin/` | 4 | 0.005102 | **0.9290** | 0.8818 | 0.9246 | 0.9714 | -0.46 |
| `g3p128` | `exp/ribsegv2/grid-g3p128/` | 4 | 0.005102 | **0.9238** | 0.8769 | 0.9203 | 0.9641 | -0.98 |
| `g4p1024` | `exp/ribsegv2/grid-g4p1024/` | 4 | 0.01024 | **0.9311** | 0.8836 | 0.9249 | 0.9608 | -0.26 |
| `g4p1024-lrpin` | `exp/ribsegv2/grid-g4p1024-lrpin/` | 4 | 0.005102 | **0.9314** | 0.8836 | 0.9273 | 0.9680 | -0.23 |
| `g4p128nf` | `exp/ribsegv2/grid-g4p128nf/` | 4 | 0.009117 | **0.9276** | 0.8800 | 0.9240 | 0.9630 | -0.60 |
| `g4p128nf-lrpin` | `exp/ribsegv2/grid-g4p128nf-lrpin/` | 4 | 0.005102 | **0.9280** | 0.8799 | 0.9224 | 0.9661 | -0.56 |

Read the `-lrpin` rows: three of the four arms drew a higher lr than `augc-all5`, the 3mm-fp32 cell they are all measured against. At matched lr the grid is 3mm 0.9336 (fp32/reduced) > 4mm/full/bf16 0.9314 > 3mm/full/bf16 0.9290 > 4mm/reduced/fp32 0.9280 > 3mm/reduced/bf16 0.9238, and each factor is now clean in isolation: **bf16 costs 0.98 pt**, **full windows gain 0.52 pt**, **4mm costs 0.56 pt**. So **keep 3mm and keep fp32** (restored, on the one-variable fp32/reduced comparison), but **"`enc_patch_size` is irrelevant" was wrong** -- the original -0.02 tie was two opposing errors cancelling. It changes nothing in practice: full windows in fp32 are unreachable (no flash means the attention matrix is materialised, memory linear in patch size, OOM at 8x), so fp32 forces reduced windows, and the +0.52 never covers the -0.98. The factors are still not additive -- stacking all three predicts -1.02 and measures -0.23 -- but the earlier claim that each *flips sign* depending on the other was itself an lr artefact. 2.5mm stays excluded on measurement regardless: 96% of a 22GB card AND stage4_max 1042 > 1024, which loses the single global attention window at the deepest stage.

## Step 4 -- optimisation knobs

Config: [configs/ribsegv2/seq-tune/opt.py](../configs/ribsegv2/seq-tune/opt.py) -- the `mode` column is its `TUNE_MODE`

| mode | log path | batch | lr | dice_fg | iou_fg | recall_fg | label_acc | Δ dice_fg |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `dp0.1` | `exp/ribsegv2/opt-dp0.1/` | 4 | 0.005102 | **0.9280** | 0.8804 | 0.9231 | 0.9631 | -0.56 |
| `dp0.5` | `exp/ribsegv2/opt-dp0.5/` | 4 | 0.005102 | **0.9336** | 0.8868 | 0.9292 | 0.9722 | -0.00 |
| `dp0.5-s7` | `exp/ribsegv2/opt-dp0.5-s7/` | 4 | 0.005102 | **0.9295** | 0.8823 | 0.9256 | 0.9704 | -0.41 |
| `lr2x` | `exp/ribsegv2/opt-lr2x/` | 4 | 0.0115 | **0.9078** | 0.8579 | 0.9013 | 0.9421 | -2.58 |
| `ls0.1` | `exp/ribsegv2/opt-ls0.1/` | 4 | 0.005102 | **0.9291** | 0.8813 | 0.9229 | 0.9673 | -0.45 |
| `ls0.2` | `exp/ribsegv2/opt-ls0.2/` | 4 | 0.005102 | **0.9262** | 0.8798 | 0.9211 | 0.9629 | -0.74 |
| `ref` | `exp/ribsegv2/opt-ref/` | 4 | 0.005102 | **0.9300** | 0.8823 | 0.9250 | 0.9665 | -0.36 |
| `ref-s7` | `exp/ribsegv2/opt-ref-s7/` | 4 | 0.005102 | **0.9302** | 0.8835 | 0.9260 | 0.9650 | -0.34 |
| `wd5e-2` | `exp/ribsegv2/opt-wd5e-2/` | 4 | 0.005102 | **0.9300** | 0.8826 | 0.9252 | 0.9679 | -0.37 |
| `wd5e-3` | `exp/ribsegv2/opt-wd5e-3/` | 4 | 0.005102 | **0.9297** | 0.8817 | 0.9272 | 0.9658 | -0.39 |

On the standing recipe (`all5`), batch pinned to 4 and lr pinned to 0.005102. **Compare against `ref`, not `augc-all5`** -- `ref` re-runs the standing recipe on this node and reproduces it to -0.36 pt, so using it as the baseline cancels the node offset. `lr2x` is the deliberate exception, run at 0.0115 to price the lr confound: it came back **-2.22 pt**, the number the whole audit rests on.

**Every knob is a tie: label smoothing, weight decay and drop_path all leave the result unchanged.** Weight decay is the flattest -- a 100x sweep (5e-4 -> 5e-2) moves the fourth decimal, which is a stronger null than scattered ties would be. `dp0.5` looked like the one winner at +0.36 pt with every metric agreeing, so it got a second seed: the two `dp0.5` runs differ by **0.41 pt from each other**, independently reproducing the 0.40 pt noise floor, and the mean lands +0.16. The first seed was a high draw. Read `-s7` rows as second seeds, not variants.

Taken with steps 1 and 3, this is the pattern of the whole sweep: input features inert, optimisation knobs inert, **augmentation worth +2.96 pt**. On 320 training volumes the limit is data variety, not regularisation.

## FINAL -- the tuned recipe, 100 epochs, TEST split

Config: [configs/ribsegv2/seq-tune/final.py](../configs/ribsegv2/seq-tune/final.py)

| mode | log path | batch | lr | dice_fg | iou_fg | recall_fg | label_acc | Δ dice_fg |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `-` | `exp/ribsegv2/final/` | 4 | 0.005102 | **0.9264** | 0.8796 | 0.9214 | 0.9611 |  |

The one and only read of the 157-volume test split; every decision above was made on val. Against the baseline at the top of this table (same split, same 157 volumes, same `add_trainval_incomplete=False`): **dice_fg 0.8819 -> 0.9264, +4.44 pt**; iou_fg +5.07; label_acc +5.15. Mislabelled rib points fall from 1,895,622 to 206,294, a **89% reduction**. Val for the same checkpoint was 0.9306, so the test number sits 0.42 pt below it -- roughly the noise floor, which is the evidence that ~35 runs of selection on val did not overfit it. 100 epochs bought +0.05 pt on val over 50, so the shortcut used for every tuning run cost nothing.

## Discarded runs

| run | why |
| --- | --- |
| `exp/ribsegv2/_discarded-aug-hujitter-bs2/` | ran at batch 2 (silent batch-size drop); rerun as `aug-hujitter` |
| `exp/ribsegv2/_discarded-augc-all5alt-bs2/` | ran at batch 2 (silent batch-size drop); rerun as `augc-all5alt` |

<!-- results:end -->

# LR Range Test Instability (Critical Finding)

**The lr range test is not stable — this affects every delta recorded before the fix.**

`tools/lr_range_test.py` was treated as producing a per-arm optimum, so every run from step 2 onward drew its own lr. It does not. Two configs that resolve **byte-identical** (`augc-all5` and the first `opt-ref` attempt) got **0.0051 and 0.0115 — 2.25×**. Their loss curves agree to three decimals up to lr 0.012, then one hits a transient spike (grad-norm 3–5, loss 1.64) and never recovers its minimum while the other keeps improving to 0.027. `lr_min_loss` therefore lands on whichever side of the spike that single noisy pass happened to fall. Both curves agree on where training actually blows up (~0.08); the heuristic just isn't reading that.

Across steps 2–3 the drawn lr spanned **4×** (0.0045–0.0183) between arms that were supposed to differ only in the factor under test. Steps 1 and 1b are unaffected — they pin lr at 0.005719 for every run.

Fix: `FIXED_LR` in `tmp-ai-tune_hp.sh`, the exact counterpart of `FIXED_BS`, and **both are now mandatory for any comparison sweep**.

### What it costs: −2.22 pt

Measured, not estimated. `opt-ref` and `opt-lr2x` are the same recipe at 0.005102 and 0.0115; nothing else differs:

| run | lr | val dice_fg | iou_fg | label_acc |
| --- | --- | --- | --- | --- |
| `opt-ref` | 0.005102 | 0.9300 | 0.8823 | 0.9665 |
| `opt-lr2x` | 0.0115 | 0.9078 | 0.8579 | 0.9421 |

**−2.22 pt from the lr draw alone** — larger than every augmentation effect in step 2, and 5.5x the noise floor.

`opt-ref` also reproduced `augc-all5` (0.9300 vs 0.9336, −0.36 pt, inside the floor), so the A5500 and the L4 agree and no node correction is needed.

### Which conclusions this broke (all were losers, none were unfairly flattered)

Every confounded arm drew a **higher** lr than the reference it lost to, so every one was penalised and its true score is ≥ what was recorded.

| arm | lr vs ref | Δ recorded | status |
| --- | --- | --- | --- |
| `augc-all5alt` | 2.68x | −2.67 | **confounded** — the gap ≈ the lr penalty |
| `aug-rotz` | 2.39x | +0.60 | confounded, but rotz is already in `all5` |
| `grid-g3p1024` | 2.12x | −1.00 | **confounded** — may actually be a win |
| `grid-g4p1024` | 2.01x | −0.26 | **confounded** |
| `augc-top3` | 1.89x | −0.98 | **confounded** — likely a tie with `all5` |
| `grid-g4p128nf` | 1.79x | −0.60 | mild |
| `aug-truncrib` | 1.42x | +1.25 | mild; already in `all5` |
| everything else | ≤1.30x | | clean |

**Step 2b resolved by re-runs at matched lr:** `all5` 0.9336 > `scale` 0.9269 > `all5alt` 0.9258 > `top3` 0.9230. `all5` wins by 0.67 pt.

**Step 3 resolved by re-runs at matched lr:** each factor clean in isolation — bf16 costs 0.98 pt, full windows gain 0.52 pt, 4mm costs 0.56 pt. The claim "`enc_patch_size` is irrelevant" was wrong (original −0.02 tie was two opposing errors cancelling).

The cliff is between lr 0.0097 and 0.0115, not a gradient in the ratio.

# Operational Notes

## Running an experiment

```bash
singularity exec --nv /share/tliang/pointcept.sif \
    env FIXED_BS=4 FIXED_LR=0.005102 bash tmp-ai-<step>.sh
```

- **`FIXED_BS=4` and `FIXED_LR=0.005102` are both mandatory** for anything compared against the runs above. Same failure mode in both cases: the value is measured per run, the measurement carries variance far larger than the effects under test, and the result looks valid but is not comparable.
  - batch: memory-determined, ~5-10% variance; near the ceiling that silently halves the batch for one arm and not another. Happened twice, cost 2.5 and 6.5 pt.
  - lr: 4x spread across steps 2-3, worth 2.22 pt measured. Do **not** re-derive it from `lr_range_test` for a comparison arm. 0.005102 is what the standing recipe trained at.
- `MEMFRAC` (default 0.9) only matters if re-measuring batch size.
- Needs 2 free GPUs, >=18 GiB each. No Ampere requirement (fp32 fallback path).
- Both discarded bad runs are kept as `exp/ribsegv2/_discarded-*-bs2`.
- `tmp-ai-lrpin.sh` is the generic "re-run these arms at the pinned lr" driver: `CFG=seq-tune/<cfg> PREFIX=<exp prefix> MODES="a b c"`.

## Operational gotchas

- **Never edit a running `.sh` in place** — bash reads scripts lazily by byte offset. Write to a temp file and `\mv -f` it over (mv is aliased to `mv -i`).
- **`pgrep -f <pattern>` wait loops self-match**, including the parent shell whose command line contains the pattern. One waited forever that way.
- `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` is a no-op on these nodes.
- `scripts/test.sh` uses `PYTHONPATH=./` so it picks up current code, but reads the run's **saved** `config.py` — pass split overrides with `-o` (see `tmp-ai-val_test.sh`).
- `scripts/train.sh` now decides "resume" on `model_last.pth`, not on the exp dir existing, so tuning output can be written there before training starts.

# The Pattern Across All Four Steps

Input features inert (step 1), optimisation knobs inert (step 4), grid and precision worth ~1 pt at most (step 3) — and **augmentation worth +2.96 pt** (step 2). On 320 training volumes the binding constraint is data variety, not regularisation or optimisation. That is also where the remaining headroom is: the baseline diagnosis was complete cages 0.916 vs incomplete 0.681.
